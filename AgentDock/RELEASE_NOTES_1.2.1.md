# ClaudeCodeManager 1.2.1

## 中文术语与主界面整理

- 统一采用“多智能体”名称，首次说明为“多智能体（Agent Teams）”。
- 底层环境变量 `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` 保持不变。
- 主界面右侧操作区调整为：

```text
检查更新 / 桌面快捷
导入配置 / 打开配置
模型配置 / 多智能体
```

- 写入用户级 `CLAUDE.md` 的托管规则同步采用统一中文名称。

## 下载

- `ClaudeCodeManager-1.2.1.exe`：独立可执行文件。
- `ClaudeCodeManager-1.2.1-windows-x64.zip`：包含 EXE、README、CHANGELOG、LICENSE。
